// Đây là nơi bạn có thể định nghĩa schema nếu bạn cần làm việc với cơ sở dữ liệu
// Ví dụ với schema cho tin nhắn

type Message = {
  id: string;
  content: string;
  sender: string;
  timestamp: string;
};
